import { bootstrapApplication } from '@angular/platform-browser';
import * as appComponent from './app/app.component';


const bootstrap = () => bootstrapApplication(appComponent.AppComponent);

export default bootstrap;
